extend type Query {
  listConcepts(
    org: String!
    source: String!
    verbose: Boolean!

    # optional params
    q: String
    sortAsc: String
    sortDesc: String
    conceptClass: String
    dataType: String
    locale: String
    includeRetired: Boolean
    includeMappings: Boolean
    includeInverseMappings: Boolean
  ): [Map!]!
}
